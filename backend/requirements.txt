# -*- coding: utf-8 -*-
# ^ DÒNG TRÊN KHÔNG PHẢI TRANG TRÍ, ĐỪNG XOÁ. pip đọc file này bằng auto_decode(): không có
# BOM và không có khai báo encoding kiểu PEP-263 ở HAI DÒNG ĐẦU thì nó decode bằng
# locale.getpreferredencoding() - trên Windows tiếng Việt là cp1252/cp1258, và mọi chú thích
# tiếng Việt bên dưới làm `pip install -r requirements.txt` NỔ UnicodeDecodeError ngay bước
# cài đặt (setup.bat [2/3]). chcp 65001 KHÔNG cứu được: nó đổi codepage của console, còn
# locale.getpreferredencoding() đọc ANSI codepage của hệ thống.
# QUAN TRỌNG: fastapi/starlette đã được NÂNG CẤP có chủ đích lên fastapi==0.141.1 /
# starlette==1.6.0 (2026-08-17) để cài được google-adk==2.7.0 thật (yêu cầu
# starlette>=1.3.1 cho module google.adk.workflow — xem docs/agent-platform/ADK_INTEGRATION.md).
# Đã verify bằng `pip install --dry-run` trên chính .venv này: claude-agent-sdk's mcp
# dependency chỉ cần starlette>=0.27 (python_version<3.14), KHÔNG có upper bound xung đột;
# sse-starlette chỉ cần anyio. Không còn lý do giữ fastapi ở 0.115.0.
fastapi==0.141.1
uvicorn[standard]>=0.32.0
# httpx bumped 0.27.2 -> 0.28.1 (2026-08-17, alongside the fastapi/starlette bump above):
# google-genai (a hard dep of google-adk==2.7.0, every version in its allowed range
# 2.12.1-2.18.1) requires httpx>=0.28.1. 0.28.1 is also the highest version that still
# satisfies the supabase stack's httpx>=0.26,<0.29 (postgrest/storage3/supabase-auth/
# supabase-functions) plus mcp's >=0.27.1,<1.0.0, openai's >=0.23.0,<1, and opensandbox's
# >=0.27.0,<1.0 - verified with a real (non-dry-run) `pip install -r requirements.txt`.
httpx==0.28.1
claude-agent-sdk==0.2.116
starlette==1.6.0
sse-starlette>=1.6.1,<3
python-dotenv==1.0.1
# websockets bumped 13.1 -> 15.0.1 (2026-08-17, alongside the fastapi/starlette bump above):
# google-adk==2.7.0 requires websockets<16,>=15.0.1. realtime==2.31.0 (a supabase dep) only
# needs websockets>=11,<16, so 15.0.1 is the highest version satisfying both — verified with
# a real (non-dry-run) `pip install -r requirements.txt`, which is how this conflict was
# actually caught (the dry-run noted in the fastapi/starlette comment above did not surface it).
websockets==15.0.1
edge-tts>=7.0.0
python-multipart==0.0.18
pyyaml>=6.0
cryptography>=42.0
jsonschema>=4.20,<5
# uv: KHÔNG phải thư viện app import, mà là runner cho các connector MCP khai `command: uvx`
# trong system/mcp-catalog.json (google-keep, google-sheets, google-search-console, google-ads,
# tiktok-ads). Cài qua đây là đủ cho cả Docker lẫn máy cá nhân - `pip install uv` sinh ra sẵn
# lệnh `uvx`. ĐỪNG gỡ dòng này, mấy connector đó sẽ chết lặng lẽ.
uv>=0.5
# gpsoauth: đổi Google App Password -> master token NGAY TRÊN UI (server/cred_exchange.py), để
# connector google-keep khỏi bắt user mở terminal chạy lệnh. Kéo theo pycryptodomex/requests/
# urllib3; đã kiểm bằng `pip install --dry-run`: KHÔNG đụng tới pin fastapi/starlette ở trên.
gpsoauth>=1.0
# passlib 1.7.4 dò backend bcrypt bằng thuộc tính __about__.__version__ - bcrypt>=4.1 đã bỏ
# thuộc tính này nên passlib crash ValueError ngay lần hash/verify đầu tiên. Ghim bcrypt<4.1
# (bản cộng đồng xác nhận tương thích) cho tới khi passlib ra bản vá.
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
pyjwt>=2.8.0
boto3>=1.34.0
livekit-api>=1.0
alembic>=1.13.0
openai>=1.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pytest>=8.0.0
# Required for @pytest.mark.asyncio in test_events.py (Phase 4 SSE stream) and
# test_voice.py - without it pytest fails every async test with "async def
# functions are not natively supported", it does not skip them silently.
pytest-asyncio>=0.23.0
pgvector>=0.2.0
# Chỉ dùng cho nhánh LISTEN/NOTIFY của chat streaming (app/services/chat_stream_bus.py) -
# SQLAlchemy/psycopg2 sync ở trên vẫn là ORM chính, asyncpg không thay thế nó.
asyncpg>=0.29.0
# Agent Runtime Phase 1 (backend/app/agents/runtime/adapters/deepseek_harness.py) - SDK
# Developer Preview thật của deepseek-ai/deepseek-harness, kéo theo gói nhị phân
# deepseek-harness-runtime-bin==0.1.0rc6 (~52MB, subprocess JSON-RPC). Ghim đúng bản đã
# kiểm tra API; DeepSeekHarnessAdapter import lazy nên thiếu gói này KHÔNG chặn app boot,
# chỉ khiến runtime "deepseek_harness" báo unavailable. Không phải dependency bắt buộc cho
# CI mặc định (MockRuntime mới là default test path).
deepseek-harness-sdk==0.1.0rc6
# OpenSandbox Execution Runtime Phase 1 (backend/app/agents/execution/adapters/opensandbox.py) -
# Ghim cứng bản 0.1.15 vì SDK 0.1.x đang trong giai đoạn phát triển tích cực và API có thể thay đổi.
# OpenSandboxExecutor import lazy: thiếu gói này không chặn app boot mà chỉ báo health unavailable.
opensandbox==0.1.15
# DSPy Intelligence Program & Optimization Layer (COSA v13.1/v13.2) -
# Ghim cứng dspy==3.3.0 để đảm bảo tính tái lập (reproducibility) của các compiled artifacts.
# Runtime import lazy: thiếu gói này không chặn boot COSA Core (chuyển sang fallback runtime).
dspy==3.3.0

# LiteLLM — đã có sẵn transitively qua google-adk (litellm==1.97.0), ghim trực tiếp
# vì backend/app/workforce/agents/reliability/litellm_invoker.py import thẳng nó.
litellm==1.97.0
greenlet>=3.0.0
google-adk==2.7.0

# OpenTelemetry Observability & Distributed Tracing (§Phase 6)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

