[pytest]
pythonpath = . packages apps
testpaths = tests/agent_core tests/apps

asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests as integration tests
filterwarnings =
    ignore::DeprecationWarning

