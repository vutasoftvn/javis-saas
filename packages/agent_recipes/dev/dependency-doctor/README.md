# Recipe: Dependency Doctor

Pattern `critic-revise` (Blueprint V2 §70) — 1 lượt đề xuất + 1 lượt tự phê bình trước khi chốt khuyến nghị.

## Trạng thái phụ thuộc (2026-08-24)

`web.search` (tra cứu CVE/changelog) chưa implement — giống các recipe khác cần capability này. CVE database lookup cụ thể (nguồn dữ liệu nào, rate limit) chưa quyết định — để lại cho lúc triển khai thật.
