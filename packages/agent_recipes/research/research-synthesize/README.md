# Recipe: Research & Synthesize

Pattern nền tảng (`research-synthesize`, Blueprint V2 §70) — `sales/competitor-intelligence` là 1 chuyên biệt hoá của pattern này cho domain sales. Tạo recipe domain khác (vd `finance/market-report`) bằng cách kế thừa cùng `workflow.pattern`, đổi `requires.capabilities`/`requires.skills` cho phù hợp.

## Trạng thái phụ thuộc (2026-08-24)

Giống `sales/competitor-intelligence` — `web.search` chưa implement, skill `evidence-synthesis` đã có sẵn trong `skillpacks/`.
