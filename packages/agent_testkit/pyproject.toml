[project]
name = "agent-testkit"
version = "0.1.0"
description = "Conformance test suite dùng chung cho mọi ExecutionKernel/ModelProvider/WorkflowRuntime/Gateway/persistence/protocol implementation mới."
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
