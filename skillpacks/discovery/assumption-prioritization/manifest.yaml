apiVersion: agentos.ai/v1
kind: Skill
metadata:
  id: discovery.assumption-prioritization
  name: Assumption Risk Matrix Prioritization
  version: 1.0.0
  description: Xếp hạng ưu tiên giả định theo mức độ rủi ro tử huyệt (fatal if wrong)
    và mức độ thiếu bằng chứng.
publisher:
  name: javis
  type: official
source:
  type: local
  path: skillpacks/discovery/assumption-prioritization
capability:
  domain: discovery
  category: discovery
  intents:
  - assumption risk matrix prioritization
  - discovery.assumption-prioritization
applicability:
  project_stages:
  - P2_SOLUTION_VALIDATION
  gates:
  - G2
  required_context:
  - workspace
  - project
  outputs:
  - artifact
  - proposal
autonomy:
  ceiling: L1_PROPOSE
  side_effect_class: A
evidence:
  min_source_refs: 1
  self_validation_forbidden: true
quality:
  eval_suite: evals/discovery/assumption-prioritization.yaml
  required_negative_cases:
  - missing-workspace
  - cross-workspace
runtime:
  entrypoint: SKILL.md
  tools: []
permissions:
  required:
  - READ_LOCAL
risk:
  level: low
trust:
  tier: T0
