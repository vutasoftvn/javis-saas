apiVersion: agentos.ai/v1
kind: Skill

metadata:
  id: marketing.seo-plan
  name: SEO Strategy, Content Architecture & AI Visibility
  version: 1.1.0
  description: Xây dựng chiến lược SEO toàn diện, phân nhóm cụm từ khóa theo ý định tìm kiếm, tối ưu khả năng hiển thị AI-Search, checklist kiểm toán kỹ thuật và cấu trúc dữ liệu schema.

publisher:
  name: javis
  type: official

source:
  type: local
  path: skillpacks/marketing/seo-plan

capability:
  domain: marketing
  category: seo
  intents:
    - seo plan
    - keyword strategy
    - search intent clustering
    - content prioritization
    - ai search optimization
    - technical seo audit
    - schema structured data

runtime:
  entrypoint: SKILL.md
  tools:
    - web.search

permissions:
  required:
    - READ_LOCAL

risk:
  level: low

trust:
  tier: T0
