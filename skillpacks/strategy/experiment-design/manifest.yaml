apiVersion: agentos.ai/v1
kind: Skill

metadata:
  id: strategy.experiment-design
  name: Lean Experiment Design & Hypothesis Testing
  version: 1.1.0
  description: Thiết kế thử nghiệm kiểm chứng giả định chiến lược (smoke test, landing page, concierge, prototype, interview) với khung giả thuyết định lượng và cây quyết định phân nhánh.

publisher:
  name: javis
  type: official

source:
  type: local
  path: skillpacks/strategy/experiment-design

capability:
  domain: strategy
  category: experiment
  intents:
    - thiết kế thử nghiệm
    - experiment design
    - kiểm chứng giả định
    - hypothesis testing
    - a/b testing
    - smoke test design
    - success metrics

runtime:
  environment: python
  entrypoint: SKILL.md
  tools:
    - strategy.experiment.create

permissions:
  required:
    - READ_LOCAL
    - MODIFY_BUSINESS_DATA

risk:
  level: medium

trust:
  tier: T0
