apiVersion: agentos.ai/v1
kind: Skill
metadata:
  id: strategy.business-model
  name: Business Model Canvas & Value Dynamics
  version: 1.0.0
  description: Thiết lập cấu trúc mô hình kinh doanh ban đầu (Lean / Business Model
    Canvas) và luồng giá trị kinh tế.
publisher:
  name: cosa
  type: official
source:
  type: local
  path: skillpacks/strategy/business-model
capability:
  domain: strategy
  category: strategy
  intents:
  - business model canvas & value dynamics
  - strategy.business-model
applicability:
  project_stages:
  - P0_DISCOVERY
  gates:
  - G0
  required_context:
  - workspace
  - project
  outputs:
  - artifact
  - proposal
autonomy:
  ceiling: L1_PROPOSE
  side_effect_class: A
evidence:
  min_source_refs: 0
  self_validation_forbidden: true
quality:
  eval_suite: evals/strategy/business-model.yaml
  required_negative_cases:
  - missing-workspace
  - cross-workspace
runtime:
  entrypoint: SKILL.md
  tools: []
permissions:
  required:
  - READ_LOCAL
risk:
  level: low
trust:
  tier: T0
