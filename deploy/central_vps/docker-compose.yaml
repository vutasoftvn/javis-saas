name: cosa

services:
  # ============================================================================
  # Central PostgreSQL 16 (Database độc lập cho COSA Central Control Plane)
  # ============================================================================
  postgres:
    image: postgres:16-alpine
    container_name: cosa_db
    restart: unless-stopped
    ports:
      - "127.0.0.1:5434:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-cosa}
      POSTGRES_USER: ${POSTGRES_USER:-cosa_central_admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-SecureCentralPass2026}
    volumes:
      - central_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cosa_central_admin} -d ${POSTGRES_DB:-cosa}" ]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  central_pgdata:
